<resources>
    <!-- Base application theme. -->
    <style name="Base.Theme.TriosoftApp" parent="Theme.Material3.Light.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.App.Starting" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/white</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/graphapp_logo_splash</item>
        <item name="windowSplashScreenAnimationDuration">2000</item>
        <!-- Set the theme of the Activity that directly follows your splash screen. -->
        <!-- Required -->
        <item name="postSplashScreenTheme">@style/Theme.Material3.Light.NoActionBar</item>

    </style>

    <style name="Theme.TriosoftApp" parent="Base.Theme.TriosoftApp" />
</resources>